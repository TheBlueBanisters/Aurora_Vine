# Aurora Vine（极光藤）项目 Proposal

> 本文档概述项目定位、技术实现、功能现状与待扩展方向，供立项、评审或对外说明使用。  
> **功能与实现细节以仓库根目录 `README.md` 及源码为准**；`功能汇总.md` 为早期草稿，部分内容已过时。

---

## 1. 项目概述

**Aurora Vine** 是一款面向留学申请者的 **Electron 桌面应用**，旨在帮助用户整理学术背景、浏览与收藏院校、查阅申请案例、管理留学时间线与每日任务，并在本地社区中交流。产品口号：**让留学规划更清晰（A Clearer Path to Studying Abroad）**。

**形态**：单机桌面客户端，数据以 **本地 SQLite** 与 **浏览器 localStorage** 为主，无需强制联网即可使用核心功能（院校与案例数据通过离线脚本导入）。

---

## 2. 目标与价值（Proposal 叙事要点）

- **信息整合**：将院校目录、专业列表、申请案例等结构化沉淀在本地数据库中，便于检索与对比。
- **个人背景数字化**：通过定校规划表单与可视化，将 GPA、标化、软背景等转为可回顾、可量化的档案。
- **执行落地**：留学规划大纲（文本解析入库）与每日打卡联动，把长期目标拆解为按日任务。
- **社区与身份**：本地账户体系、邀请码认证、社区发帖与回复，形成轻量闭环（无云端服务依赖）。

**当前边界**：不提供云端同步、不内置大模型 API；「智能留学规划」等为产品预留占位，需在路线图中单独立项。

---

## 3. 技术架构

| 层级 | 实现 |
|------|------|
| 桌面壳 | Electron 33 |
| 构建 | electron-vite 3.x（主进程 / preload / 渲染进程分包构建） |
| 渲染层 | 原生 HTML / CSS / **JavaScript（ES 模块）**，按功能拆分于 `renderer/modules/`，无 React / Vue |
| 进程通信 | `preload.js` 通过 `contextBridge` 暴露 `window.api`；渲染进程 **不开启 Node 集成**，**contextIsolation** 启用 |
| 数据 | **better-sqlite3**，单库 `data/school_item.db` |
| 图表 | ECharts 5.x、echarts-gl（依赖已引入；当前「我的背景」等以 2D 图表为主） |
| 通知 | NotifyX |
| 数据准备 | `data/init_db.js` + xlsx（专业表）、CSV（案例，GB18030 解码） |

**自定义协议**：`school://No.{QS排名}/...` 读取院校素材；`avatar://account/{id}` 读取用户头像。

**开发体验**：`npm run dev` 下 Vite 提供 HMR；`/image` 映射到项目 `image/` 便于静态资源加载。

---

## 4. 功能现状（已实现）

### 4.1 启动与账户

- **启动页**：产品介绍（中英）、轮播图；入口为 **登录 / 注册 / 游客**。
- **账户**：邮箱注册与登录、游客模式；会话与账户表存于 SQLite；**邀请码认证**（学长/学姐类型，邀请码由主进程配置）；**头像**上传（格式与大小由主进程校验），通过自定义协议引用。

### 4.2 定校规划与我的背景

- **定校规划**：多段表单（毕业届、院校层次、校名专业、GPA 与分制与排名百分位、雅思/托福/GRE 及「无」选项、科研/实习/论文数量等）；校验通过后写入 **localStorage**（按账号/游客分键，支持游客数据向账号迁移提示）。
- **结果呈现**：前端 **`scoring.js`** 加权模型计算综合分与各维度分，**环形进度 + 分项展示**（非院校推荐列表）。
- **我的背景**：展示已填背景；**ECharts** 横向条形图对比标化相关指标；无数据时引导至定校规划。

**限制**：简历字段仅记录 **文件名**，无文件落盘与解析；**无自动选校/冲刺匹配名单**。

### 4.3 院校与目标列表

- **院校浏览**（侧栏「院校数据库」页）：分页列表、**关键词搜索**、**地区筛选**（含英美新港欧等）；卡片进入详情。
- **热门榜单筛选**：当前仅 **QS** 可用；**Times / U.S.News** 为 UI 占位（敬请期待）。
- **目标院校**：收藏列表（localStorage），按 **QS 排名**排序。
- **院校详情**：覆盖层展示 intro、官网、地址、轮播与灯箱；**专业列表**（SQLite）；**该校相关申请案例**入口。

### 4.4 留学规划与每日打卡

- **留学规划大纲**：用户按约定格式编写文本（`#标题`、`*简介`、`[起止日期]任务`），前端解析后通过 IPC 批量写入 **`study_plan` 表**；列表展示与删除；解析成功后可按日期将任务 **追加至每日打卡**。
- **智能留学规划（AI）**：界面为 **占位**，无模型调用与生成逻辑。
- **每日打卡**：月历 + 当日任务；每日最多 **9** 条；完成状态与颜色；支持追加、按日清空、**全局清空**（确认后执行）；数据在 SQLite。

### 4.5 申请案例

- 分页、关键词搜索、本科层级 / GPA 区间 / 语言 / 软背景 / 排序等多维筛选；详情弹窗（背景、标化、软背景、Offer 列表）；与院校详情联动。数据由 `init_db.js` 自 CSV 等导入。

### 4.6 社区留言

- 发帖、回复、嵌套评论；用户可删除自己的帖子与回复；数据存 SQLite。

### 4.7 资源中心

- **整页占位**（即将上线文案），无实际资源或下载能力。

### 4.8 设置与其他

- 夜间模式（与主进程窗口 `theme:apply` 同步）、个人信息、头像、邀请码、关于等。
- **国际化**：中 / 英（`i18n.js` + `data-i18n`）。
- **新手指引**：首次进入等场景的引导流程（部分文案可能与真实进度不完全同步）。

---

## 5. 数据与初始化

- **数据库文件**：`data/school_item.db`（首次需执行 `node data/init_db.js`；**重建会清空库内所有运行期数据**，重要数据需先备份）。
- **导入来源**：`school/No.{QS排名}/`（`intro.json`、校徽与图片等）；可选 `major/*.xlsx` → `school_programs`；可选 `personalCase/*.csv` → 申请案例相关表。
- **localStorage**：定校规划、目标院校 ID、主题、语言等。

---

## 6. 占位与后续可立项方向

| 方向 | 现状 |
|------|------|
| 智能留学规划（AI） | 仅 UI 占位 |
| 资源中心 | 仅占位页 |
| 多榜单排序（Times / U.S.News） | 控件禁用，未接数据 |
| 简历深度利用 | 仅文件名，无解析 |
| 定校「院校推荐」 | 仅有综合分，无匹配算法与列表 |
| 可选：云端同步、在线社区、LLM 接入 | 未实现，需单独架构与安全评估 |

---

## 7. IPC 能力摘要（`window.api`）

涵盖：`auth:*`、`avatar:getDataUrl`、`schools:*`（列表/搜索/详情/专业/素材）、`applicationCases:*`、`dailyCheckin:*`、`studyPlan:*`、`community:*`、`theme:apply`。具体通道名与参数以 `preload.js` 与 `main/ipc/*.js` 为准。

---

## 8. 小结

Aurora Vine 以 **Electron + 本地 SQLite** 实现留学场景下的 **离线数据浏览、个人背景建档、规划文本解析与打卡联动、轻量社区**，适合作为课程项目、工具类课题或后续接入 AI/云服务的 **可演示基座**。资源中心与 AI 规划等需在 proposal 中明确列为 **下一阶段**，与当前交付范围区分。

---

*文档随仓库演进可更新；技术细节请参阅 `README.md`。*
